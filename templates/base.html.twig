<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ATLANTIK{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/superhero/bootstrap.min.css">
        <link rel="stylesheet" href="../css/app/customcss.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>

          <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="/">ATLANTIK</a>
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>

            <div class="collapse navbar-collapse" id="navbarColor02">
              <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                  <a class="nav-link" href="/">Accueil <span class="sr-only">(current)</span></a>
                </li>

                {# Appel de la 'GROSSE' variable d'environement Twig APP #}
                {% if not app.user %}
                  <li class="nav-item">
                    <a class="nav-link" href="">statut : déconnecté</a>
                  </li>
                {% else %}
                  <li class="nav-item">
                    <a class="nav-link" href="">statut : connecté</a>
                  </li>
                {% endif %}

              </ul>
              <form class="form-inline my-2 my-lg-0">
                {% if not app.user %}
                <div class="reservation px-2">
                  <a data-toggle="modal" href="{{ path('bateau') }}" data-target="#myModal">
                    <button type="submit" name="button" class="btn btn-success">Réservé un Trajet</button>
                  </a>
                  <div id="myModal" class="modal" tabindex="-1" role="dialog">
                    <div class="modal-dialog" role="document">
                      <div class="modal-content">
                        <div class="modal-header">
                          <h5 class="modal-title">Réservé un Trajet</h5>
                          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                          </button>
                        </div>
                        <div class="modal-body">
                          <p>Pour effectuer une réservation il faut être connecté.</p>
                        </div>
                        <div class="modal-footer">
                          <a class="btn btn-primary" href="{{ path('security_login') }}">Se connecter</a>
                          <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="nav-item">
                  <a class="btn btn-warning" href="{{ path('security_login') }}">Connexion</a>
                </div>
                {% else %}
                <div class="reservation px-2">
                  <a class="btn btn-success" href="{{ path('bateau') }}">Réservé un Trajet</a>
                </div>
                <div class="nav-item">
                  <a class="btn btn-danger" href="{{ path('security_logout') }}">Déconnexion</a>
                </div>
                {% endif %}
              </form>
            </div>
        </nav>

        <div class="container-fluid">
          <div class="container demo-content">
            <div class="row">
              <div class="col-md-12">
                {% block body %}{% endblock %}
              </div>
            </div>
          </div>
        </div>

        {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
        {% endblock %}

    </body>
</html>
